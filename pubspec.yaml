name: dairy_store_app
description: A dairy products store application

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^4.0.0
  http: 1.2.1
  shared_preferences: ^2.0.6
  collection: ^1.15.0
  video_player: ^2.5.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
    

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/products/
    - assets/back.mp4
